server {
    listen    			80;
    server_name  		104.236.19.161;
    charset			utf-8;
    client_max_body_size	75M;

    location / { try_files $uri @perks; }
    location @perks { 
        include uwsgi_params;
	uwsgi_pass unix:/var/uwsgi/perks.sock;
    }
}
